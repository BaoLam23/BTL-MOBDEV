<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Định nghĩa một style mới -->

    <style name="appProgressBar" parent="Widget.AppCompat.ProgressBar.Horizontal">
        <item name="android:indeterminateOnly">true</item>
        <item name="android:progressDrawable">@drawable/process_bar_drawable</item>
        <item name="android:minHeight">10dp</item>
        <item name="android:maxHeight">60dp</item>
        <item name="android:minWidth">10dp</item>
        <item name="android:maxWidth">141dp</item>
    </style>

</resources>